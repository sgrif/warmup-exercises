There's several fun opportunities beyond `binary-search-tree` 1 (#insert) and
2 (#each).

- #size
include Enumerable, provide a block on initialization which acts as a custom comparison, defaulting to the spaceship operator.

